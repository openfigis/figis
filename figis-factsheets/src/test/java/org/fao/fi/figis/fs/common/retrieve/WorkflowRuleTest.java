package org.fao.fi.figis.fs.common.retrieve;

import org.junit.Test;

public class WorkflowRuleTest {

	@Test
	public final void testWorkflowRule() {
		// FiConstants.setServerType(FiConstants.SERVER_TYPE_EXTERNAL);
		// boolean loggedIn = false;
		// int status = WorkflowRule.DRAFT;
		// WorkflowRule r = new WorkflowRule(loggedIn, status);
		// assertTrue(r.clear());
		// status = WorkflowRule.APPROVED;
		// r = new WorkflowRule(loggedIn, status);
		// assertTrue(r.clear());
		//
		// FiConstants.setServerType(FiConstants.SERVER_TYPE_DEVELOPMENT);
		// r = new WorkflowRule(loggedIn, status);
		// assertFalse(r.clear());
		// assertFalse(r.clear());

	}

}
